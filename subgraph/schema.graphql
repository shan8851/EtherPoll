type Topic @entity(immutable: false) {
  id:            ID!
  creator:       Bytes!
  metadataCid:   String!
  endTimestamp:  BigInt!
  yesCount:      BigInt!
  noCount:       BigInt!
}

type Vote @entity(immutable: true) {
  id:      ID!
  topic:   Topic!
  voter:   Bytes!
  support: Boolean!
}
